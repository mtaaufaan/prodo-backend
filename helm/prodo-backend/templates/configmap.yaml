apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "prodo-backend.fullname" . }}
  labels:
    {{- include "prodo-backend.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
